/// common Values for animations
/// usage:
/// import 'package:color_generator/src/domains/constants/constants.dart';
///
/// AnimationConstants.defaultAnimationDuration
/// AnimationConstants.shortAnimationDuration
class AnimationConstants {
  AnimationConstants._();

  /// default animation duration for common animations
  static const defaultAnimationDuration = Duration(milliseconds: 500);

  /// default animation duration for quick animations
  static const shortAnimationDuration = Duration(milliseconds: 300);
}
